# 新技能检查清单

- 选择一个小写连字符风格的目录名。
- 添加 frontmatter，并确保 `name` 与目录名一致，`description` 能准确描述触发场景。
- 使用 skill 模板中的全部必需章节。
- 复用共享约定，不要自行发明新的输出形状。
- 明确写出工具选择、配置选择和默认行为的优先级规则。
- 写清这个 skill 可能输出哪些失败分类。
- 只有当平台差异会改变执行方式时，才写入平台相关说明。
- 至少说明一个现有 skill 作为下游交接目标。
- 执行 `python3 scripts/validate_repo.py`。
